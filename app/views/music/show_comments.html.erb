<p>Comments for: <%= "#{@post.artist} - #{@post.song}" %></p>
<%= render :partial => "comment", :collection => @comments %>

<%= link_to "Add a Comment", :action => "add_comment", :id => params[:id]%>